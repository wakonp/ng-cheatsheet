import { StickyBottomDirective } from './sticky-bottom.directive';

describe('StickyBottomDirective', () => {
  it('should create an instance', () => {
    const directive = new StickyBottomDirective();
    expect(directive).toBeTruthy();
  });
});
